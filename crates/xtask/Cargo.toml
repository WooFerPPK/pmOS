[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "xtask"
path = "src/main.rs"

# xtask runs on the host, not wasm, so it can use std + any native
# dependency it likes. Keep dependencies tiny on purpose.
[dependencies]
