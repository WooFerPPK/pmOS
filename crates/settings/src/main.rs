// /usr/bin/settings — preferences pane.
//
// Wallpaper + fit mode, theme (light/dark), keyboard layout,
// timezone, default terminal font, about-this-system. Writes
// /etc/preferences.toml via the preferences crate. Binds
// pmd_keymap_manager (requires KEYMAP_ADMIN) for keymap switching.
//
// Populated in Phase 11 T184..T192.

fn main() {
    // stub
}
