// PMos display server.
//
// Userland program that holds DISPLAY_SERVER capability (the only
// process allowed to open /dev/fb0 and /dev/input/*). Listens on
// /run/display and speaks the wire protocol from
// `contracts/display-protocol.md`. Software compositor in v1.
//
// Populated in Phase 2 T098..T113.

fn main() {
    // stub
}
