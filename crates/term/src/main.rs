// /usr/bin/term — terminal emulator.
//
// Toolkit window hosting /usr/bin/sh as a child process with
// stdin/stdout piped to itself. ANSI/VT-minimal renderer. Reads
// terminal.font preference at startup.
//
// Populated in Phase 3 T124 and Phase 11 T191.

fn main() {
    // stub
}
