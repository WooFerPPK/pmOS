#![no_std]

//! SAB ring-buffer transport (producer + consumer halves).
//!
//! Populated in Phase 2 T035. This stub exists so that every crate
//! that depends on `ring` (kernel, toolkit, userland apps) can
//! compile before T035 lands.
