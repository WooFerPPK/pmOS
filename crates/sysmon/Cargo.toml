[package]
name = "sysmon"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "sysmon"
path = "src/main.rs"

[dependencies]
abi = { path = "../abi" }
toolkit = { path = "../toolkit" }
