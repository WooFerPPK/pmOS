// /usr/bin/sysmon — system monitor.
//
// Reads /proc for process list (pid, name, mem, open fds) and
// /proc/storage for OPFS quota info. Terminate button for selected
// process. Requires PROC_ENUMERATE + PROC_KILL_ANY.
//
// Populated in Phase 9 T170.

fn main() {
    // stub
}
