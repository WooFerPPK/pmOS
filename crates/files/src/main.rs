// /usr/bin/files — file manager.
//
// Directory browser, create/rename/move/delete, MIME-based open-with.
// Drag-drop import from host (FR-032a) via /run/host-files IPC +
// host_file_recv syscall. Export via Download menu (FR-032b).
//
// Populated in Phase 7 T151..T156.

fn main() {
    // stub
}
