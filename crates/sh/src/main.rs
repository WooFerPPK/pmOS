// /usr/bin/sh — the CLI POSIX-ish shell.
//
// Supports command execution, pipes, redirection, environment
// variables, and basic job control. Distinct from /usr/bin/shell
// (the desktop shell).
//
// Populated in Phase 3 T123 (minimal) and Phase 6 T142..T145 (full).

fn main() {
    // stub
}
