// /usr/bin/shell — the desktop shell.
//
// Ordinary userland program holding SHELL + DISPLAY_CLIENT +
// PROC_ENUMERATE + KEYMAP_ADMIN capabilities. Draws wallpaper,
// taskbar, launcher, and window chrome (client-side decorations).
// Replaceable per the Principle II layering test.
//
// Populated across Phase 3 T121..T122, Phase 4 T129..T133, Phase 11
// T185..T186.

fn main() {
    // stub
}
