//! Reserved for helpers shared across coreutil binaries. The
//! cat-only slice ships no shared code yet; later slices (ls, grep,
//! mkdir, rm, cp, mv) can add narrow utilities here without reshaping
//! the crate.
