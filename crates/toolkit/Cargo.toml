[package]
name = "toolkit"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
abi = { path = "../abi" }
display-proto = { path = "../display-proto" }

[dev-dependencies]
display-server = { path = "../display-server" }
