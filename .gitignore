# Rust
target/
*.rs.bk
*.rlib
*.prof*

# TypeScript / Node
node_modules/
# dist/ is NOT gitignored during the demo-build period: we ship a
# pre-built boot-screen demo directly in git so it can be served
# from any static host without a Rust toolchain. Once the real
# `just build` pipeline is wired in and produces kernel.wasm +
# display-server.wasm + toolkit + bundled apps, dist/ will become
# a pure build output and this line can be uncommented:
# dist/
build/
*.log
.env
.env.*
!.env.example

# Playwright
.playwright/
test-results/
playwright-report/
playwright/.cache/

# PMos package bundles (built artefacts)
*.pmpkg.tar

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code (user-local project config + skills cache)
.claude/

# xtask build intermediates
build/

# Local agent worktrees
.worktrees/
